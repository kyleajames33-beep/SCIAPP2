{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd chemquest && npm install",
  "buildCommand": "cd chemquest && npx prisma generate && npx next build",
  "outputDirectory": "chemquest/.next",
  "env": {
    "DATABASE_URL": "file:./data/app.db"
  }
}
